@{
    //Layout = null;
    var userModel = ViewBag.user as Model.QQUser_info;
}

<script src="~/Scripts/jquery-1.8.3.min.js"></script>
<script>
    $(function () {
        console.log("接收到数据");
        window.parent.sonsay("@userModel.nickname","@userModel.figureurl");
            @*function to() {
                console.log("接收到数据");
                window.parent.sonsay(@userModel.nickname);
            }*@
        })
</script>

<h1>@userModel.nickname 登陆中跳转中...........</h1>
